const initialState = {
    displayFeedback: false
}

export default (state = initialState, action = {}) => {
    switch (action.payload) {
        
    
        default: { 
            return state
        }
    }
}

